import { NgModule } from '@angular/core';
import { ReactiveFormsModule } from '@angular/forms';

export const arr:any[]=[ReactiveFormsModule]

@NgModule({
  
  imports: [arr],
  exports:[arr]

})
export class UserInfoModule { }
